###############
# Recommended #
###############

# lxml - for XML processing (XLIFF, TMX, TBX)
lxml>=2.1.0  # >=1.3.4 should work

# Faster matching in e.g. pot2po
# FIXME 0.11.0 is broken using pip so until this is fixed keep it at 0.10.2
python-Levenshtein>=0.10.2,<0.11.0
